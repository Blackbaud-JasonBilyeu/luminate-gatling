gatling {
  http {
            connectionTimeout = 10000000					# Timeout of the connection to the server (ms)
    		idleConnectionInPoolTimeoutInMs = 10000000
    		idleConnectionTimeoutInMs = 10000000
    		requestTimeoutInMs = 10000000					# Timeout of the requests (ms)
  }
}